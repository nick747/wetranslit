const Header = () => {
  return (
    <header
      id="header"
      className="p-2 flex justify-center items-center bg-accent opacity-75"
    >
      <span className="normal text-accent opacity-50 font-bold">
        This website is still work in progress!
      </span>
    </header>
  );
};

export default Header;
