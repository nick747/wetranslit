const Header= () => {
    return (
        <header id="header" className="">
            <span className="normal">This website is still work in progress!</span>
        </header>
    );
}

export default Header;