const Header = () => {
  return (
    <header
      id="header"
      className="p-2 flex justify-center items-center bg-accent  fixed top-0 right-0 left-0"
    >
      <span className="normal text-accent opacity-50 font-bold">
        This website is still work in progress!
      </span>
    </header>
  );
};

export default Header;
